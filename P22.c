/*
    Name : Adam Sheeres-Paulicpulle
    ID: 1036569
    email: asheeres@uoguelph.ca
    CIS 3490
*/

#include "helper.h"

void horspoolAlgo(Node *head) {
    char toFind[256];
    Node *iter = head;

    printf("Enter a string to find : ");
    scanf("%s", toFind);

    while(iter) {

        iter = iter->next;
    }

}